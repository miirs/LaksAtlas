# Barentswatch API credentials (OAuth2 client_credentials)
# Register at: https://www.barentswatch.no/minside/
# NEVER commit the real .env file — this is only an example template.
CLIENT_ID=your-barentswatch-client-id
CLIENT_SECRET=your-barentswatch-client-secret
